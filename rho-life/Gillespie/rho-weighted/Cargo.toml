[package]
name = "rho-weighted"
version = "0.2.0"
edition = "2021"
publish = false
description = "Weighted graph-structured lambda theories: a simulator for stochastic and quantum execution of rho theories"

# Deliberately dependency-free. See docs: the crate is a workspace member of
# f1r3node-rust and must never become a dependency of `node` or `casper`.
[dependencies]

[features]
default = []
quantum = []
# Only satisfiable inside the f1r3node-rust workspace: swaps SimpleMatcher for
# an adapter over the interpreter's own `Matcher::get`. See DR-W9.
workspace-matcher = []
